import { RedemptionEndpointConfig } from '../../helpers/redemptionConfig';

export const handleVaultQRRedemption = (redemptionConfig: RedemptionEndpointConfig) => {
  console.log('handleVaultQRRedemption', redemptionConfig);
};
