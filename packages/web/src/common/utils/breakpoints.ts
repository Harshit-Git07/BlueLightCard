const desktopScreenMinWidth = 1200;
const desktopScreenSmallMinWidth = 1024;
const tabletScreenMinWidth = 768;
const mobileScreenMinWidth = 280;

export const desktop = `${desktopScreenMinWidth}px`;
export const desktopSmall = `${desktopScreenSmallMinWidth}px`;
export const tablet = `${tabletScreenMinWidth}px`;
export const mobile = `${mobileScreenMinWidth}px`;
