import { RedemptionEndpointConfig } from '../../helpers/redemptionConfig';

export const handleGenericRedemption = (redemptionConfig: RedemptionEndpointConfig): void => {
  console.log('handleGenericRedemption', redemptionConfig);
};
