export type InputFieldProps = {
  label: string;
  disabled?: boolean;
  placeholder?: string;
  icon?: React.ReactNode;
  width?: string;
  validationType?: string;
  validationSuccessMessage?: string;
};
