import { RedemptionEndpointConfig } from '../../helpers/redemptionConfig';

export const handlePreAppliedRedemption = (redemptionConfig: RedemptionEndpointConfig) => {
  console.log('handlePreAppliedRedemption', redemptionConfig);
};
