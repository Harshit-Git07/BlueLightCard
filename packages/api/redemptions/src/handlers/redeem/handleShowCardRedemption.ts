import { RedemptionEndpointConfig } from '../../helpers/redemptionConfig';

export const handleShowCardRedemption = (redemptionConfig: RedemptionEndpointConfig) => {
  console.log('handleShowCardRedemption', redemptionConfig);
};
